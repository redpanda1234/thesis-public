\documentclass[border=10pt]{standalone}
\usepackage{fkmath}
\usepackage{tikz}
\begin{document}
\tikzset{
  partial ellipse/.style args={#1:#2:#3}{
    insert path={+ (#1:#3) arc (#1:#2:#3)}
  }
}
\begin{tikzpicture}[every node/.style={scale=1.75}]
  \draw[] (-1.5,1) -- (.5,-1);
  \draw[white, line width=8pt] (.5,1) -- (-1.5,-1);
  \draw[] (.5,1) -- (-1.5,-1);
  \draw[dotted] (-.5,0) circle (1.41);

  \draw[|-latex] (1.5,0) -- (3.5,0) node[midway, above] {$[\ \ ]$};
  \draw[dotted] (6.5,0) circle (1.41);

  % Left-hand labels
  % \node[above left] () at (-1.5, 1) {$x$};
  % \node[below left] () at (-1.5, -1) {$y$};

  % \node[above right] () at (.5, 1) {$y \overop x$};
  % \node[below right] () at (.5, -1) {$x \underop y$};

  \node (a) at (4.5, 0) {$x$};
  \node (+) at (8.75, 0) {$+$};
  \node (b) at (9.75, 0) {\raisebox{.25em}{$x^{-1}$}};

  % Draw the circular arcs
  \draw[cm={cos(90) ,-sin(90) ,sin(90) ,cos(90) ,(3 cm,5 cm)},] (5,4.9) [partial ellipse=30:150:1.15cm and -.8cm];
  \draw[cm={cos(90) ,-sin(90) ,sin(90) ,cos(90) ,(3 cm,5 cm)},] (5,2.1) [partial ellipse=30:150:1.15cm and .8cm];

  % Draw the B circle
  \draw (11.75, 1.4) [partial ellipse=30:150:1.15cm and -.8cm];
  \draw (11.75,-1.4) [partial ellipse=30:150:1.15cm and .8cm];

  \draw[dotted] (11.75,0) circle (1.41);
\end{tikzpicture}
\end{document}
